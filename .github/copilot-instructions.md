# copilot-instructions.md – FocusEyes

コーディングエージェントが参照するための仕様サマリ。  
このファイルの目的は **実装範囲とコーディング規則を明確にすること**。

---

## 1. プロダクト概要（スコープ）

- PC作業をするユーザー向けの **集中支援デスクトップアプリ**。
- ユーザーは作業前にタスクを宣言し、  
  作業中は「監視の目」に見守られながら作業し、  
  作業後に AI が成果をまとめてくれる。
- **このアプリの核は「完璧な集中管理」ではなく「毎日でなくても、戻ってきて作業することを肯定する」こと。**
- **最小要件**は以下の「必須機能」の実装。
  追加機能は時間に余裕があれば実装する。

## 必須機能（MVP スコープ）

### 1 タスク宣言

- ユーザーが **作業開始前にタスク名・簡単な説明を入力**できる。
- 宣言されたタスクは SQLite に保存。
- タスクは **1 回のセッションに対して 1 つ** を前提とする（複数タスクは非対応でよい）。

### 2 作業セッション管理

- 「作業開始」ボタンでセッション開始。
  - 開始時刻を DB に保存。
  - セッションIDを発行し以降のログと紐づける。
- 「作業終了」ボタンでセッション終了。
  - 終了時刻を DB に保存。
  - そのセッションの集中度・AI要約を生成。

### 3 実行アプリ取得・離席検知

- 一定間隔（例：5〜10秒）で **最前面アプリ名／プロセス名** を取得して記録。(Mac/WinそれぞれのOSの機能を呼び出して動作させる。)
- 同時に **無操作時間** を取得し、しきい値（例：3分以上）で「離席」と判定して記録。
- ログはセッションIDと紐付けて SQLite に保存。
- 最前面アプリから、ゲームなどの「集中を妨げるカテゴリ」を判定できる程度の情報を残すことで、AIに投げる前にデータを整える。
  - データとして残すレコードを以下に定義する。
    - timestamp（取得時刻）
    - sessionId
    - appExecutable（例: steam.exe, VALORANT-Win64-Shipping.exe）
    - browsing（例: YouTube - Google Chrome）
    - appDisplayName（例: Google Chrome, Steam）
  - また、AppCategory 列も作成し、著名なアプリ名が含まれた場合に該当するカテゴリを登録する。ルールは曖昧で、単純なキーワードマッチで良い。
    - WORK（VSCode, IntelliJ, Terminal, Word など）
    - BROWSER（Chrome, Edge, Safari…）
    - COMMUNICATION（Slack, Discord…）
    - GAME
    - ENTERTAINMENT（動画プレイヤー, YouTube視聴中っぽいタブ）
    - OTHER
    例1:  exe / bundle 名が steam, epic, leagueoflegends, valve, game, play を含んだら GAME。windowTitle に YouTube, Netflix, Twitch などが含まれたら ENTERTAINMENT。code, studio, intellij, android studio などを含んだら WORKのようにAIに分類させる

### 4 AI 評価（思想：「集中度測定」から「継続肯定」へ）

**このアプリの核は「完璧な集中管理」ではなく「毎日でなくても、とりあえず作業することを肯定する」こと。**

このアプリは「作業を続ける事実」を褒めるデザイン。強すぎる制約はかけずに、自主的なモチベーションを発揮させていくことをゴールにしている。(操作ログ細かく取るアプリは企業向けには存在はする)

### 5 todo
0. AI応答の修正(適宜)
1. 実行アプリのタスク目標との関連度設定(目標とカテゴリ分けが適切かどうかは後ほど修正する)
2. DB保存対応/週間目標対応とそのDB対応(こちらはデモデータ対応から先、実日付対応はのちほど?)
3. ブラウザタイトル取得機能用対応、バグ修正 
4. ツイート用コピペ機能。
5. セッション中のメニュータブの目を作る。できればアニメーションや通知など
6. 休憩タイマー実装, アイドル時間計測して点数影響
7. フレンド共有機能
